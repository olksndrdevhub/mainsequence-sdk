# ${PROJECT_NAME}

${PROJECT_DESCRIPTION}

## Quickstart

- **Python**: >= ${PYTHON_VERSION}
- **Install**:

```bash
# from the repo root
pip install -e .
# or, if 'uv' is available (your scripts install it when using requirements):
uv pip install -e .
